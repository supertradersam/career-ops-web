rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Placeholder until Firestore-backed Phase 2. Lock everything by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
